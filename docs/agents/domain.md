# Domain Docs

Layout: **single-context**.

Before exploration, read root `CONTEXT.md` and relevant ADRs under `backtest/docs/adr/`. Use the glossary terminology defined by `CONTEXT.md`. Explicitly flag conflicts with an existing ADR rather than silently overriding it.
